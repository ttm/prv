(resumo feito a partir das anotações prévias e em caderno, e a partir da reunião/conversa)

- Entrar em contato:
  * Artur (programador do icmc) OK
  * Thalesson (micro) e Clausius (ver o q eles tem feito) OK
  * Alan (ver alg de coarsening, formato para rede com texto, avaliação de estratégia multinível) OK

- Cogitar síntese de redes

- Recuperar publicação de dados

- Mirar estes usos da ferramenta:
  * Classificação de texto
  * Análise de rede acadêmica (com Artur)
  * meus interesses de estabilidade topológica e diferenciação da linguagem
  * nexos (em último lugar)
  * uso de estratégias multinível para visualização: comparação com contrações canônicas
  * multinível
  * para avaliar as estratégias de coarsening (aplicada na visualizacao):
      preservação das distribuições de medidas (?)
      aplicação de algoritmos: qualidade da resposta, ganho de velocidade (?)
      QUANTO O USUARIO GOSTOU
  * elaborar já algumas análises que queremos ser capaz de fazer (ou perguntas que queremos conseguir responder)?

primeira versão:
  - hover associando nodes e metanodes e histogramas (grau, clust, h/i/p)
      e mostrando metadados dos vértices
      e dados da rede em cada nível (ao menos número de vértices e arestas)
  - análises de texto
      por hora apenas o teste KS
  - além do painel de settings da análise:
      botão +- camada
      slider p separação entre camadas
  - alguns métodos de coarsening funcionando perfeitamente
  (( - query sparql dbpedia ou OBO p montar as redes ))

depois:
  - otimizar renderização com:
      figuras mais simples (ainda no babylon)
      1 triângulo por vertice, WebGL cru
        (teoricamente o mais leve possível para um dado computador?)

notas:
  - quais métodos de avaliação/análise das reduções tem prioridade? (ver as publicacoes, qualidade das comunidades, mostrar q ajudou o usuario a analisar, metricas, combinar sbj e obj)
  - quais elementos de visualização queremos? e quais elementos de interação?

buscar:
  - escrita de grafos com texto
  - tecnicas de visualizacao de redes multinivel (multilevel, multilayer, simplificada)

temas:
  - rede + texto (conservação das características, diferenciação)
    * abre várias redes
    * mostra as características: % h/i/p, PCA de medidas topológicas
    * teste KS entre texto de setores ou comunidades
    * ver com Alneu com que formato ele trabalha
  - rede bipartida (ou k-partida) + estratégias multinível de agregação
    * carrega a rede, avisa quantos vértices e arestas
    * p plotar, escolhe qual o tamanho almejado e método de contração
    * hover no meta-vértice mostra os vizinhos e/ou os vértices iniciais
    * pode abrir um meta-vértice como uma rede com os vértices iniciais ou algum tamanho especificado
  - versinus
    * pegar dados do lap ou ver como está o GMANE
    * escolhe uma lista de email -> mostra limites do BD: nn de mensagens, ambito de tempo, numero de autores
    * escolhe FPS, mensagens por segundo, símbolos para h/i/p
    * escolhe método de setorialização h/i/p
    * dá play
  ( - análise de rede real VS análise de método ML )
